import { useState } from "react";

export const ForecastItem = ( {dailyData, key} ) => {
    console.log(props.dailyData)
    return (

        <div className="item" key={props.key}>   </div>
            
    )
}